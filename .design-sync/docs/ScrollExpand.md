---
category: Landing
---

# ScrollExpand

Scroll-linked growth: maps scroll position continuously to `scale` (0.88 → 1), with `transform-origin: 50% 15%` so it opens downward. Replaces Reveal on an element rather than wrapping it.

Source: `YapprLanding/components/ScrollExpand.tsx`
